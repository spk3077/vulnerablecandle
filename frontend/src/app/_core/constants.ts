// CONSTANT Variables for Code

// CSRF INTERCEPTOR CONSTANTS
export const CSRFCOOKIE: string = "XSRF-TOKEN";
export const CSRFHEADER: string = "X-XSRF-TOKEN";

export const PATTERNS = {
    ZIP: "^[0-9]*$",
    CARDNUM: "^[0-9]*$",
    EXPIRE: "^(0[1-9]|1[012])/(2[3-9]|3[0-3])$",
    CVV: "^[0-9]*$"
 };
